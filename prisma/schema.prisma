generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
}

model Country {
    id        Int     @id @default(autoincrement())
    name      String  @unique
    capital   String
    continent String
    flagUrl   String

    @@map("Countries")
}
